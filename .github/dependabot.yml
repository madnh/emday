version: 2
updates:
  # Bumps the pinned commit SHAs (keeps the "# vX.Y.Z" comments in sync).
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  - package-ecosystem: gomod
    directory: /
    schedule:
      interval: weekly
